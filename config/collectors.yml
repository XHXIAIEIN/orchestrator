# Orchestrator 采集器配置
# 启用/禁用采集器，不需要改代码或重启容器。
# 注释掉或设为 false 即可禁用。
# 环境变量 COLLECTOR_{NAME} 优先级高于此文件。

# ── 核心采集器（默认启用）──
claude: true
browser: true
git: true
vscode: true
network: true
codebase: true

# ── 可选采集器（按需启用）──
# steam: true
# youtube_music: true
# qqmusic: true
