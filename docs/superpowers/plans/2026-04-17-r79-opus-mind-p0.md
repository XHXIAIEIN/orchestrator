# Plan: R79 opus-mind P0 Implementation

## Goal

让 `.claude/skills/md-lint/audit.py CLAUDE.md` 输出 JSON 评分 ≥ 8/11 (BORDERLINE 阈值)，且 `.claude/skills/*/SKILL.md` 任一文件被修改时 pre-write hook 自动拦截 < 阈值写入。5 个 P0 模式（linter + self-audit gate + reframe detection + tier labels + redundancy 7-pass）落到具体文件，不写半成品。

## File Map

- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/SKILL.md` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/scripts/audit.py` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/scripts/__init__.py` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/references/primitives.md` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/tests/fixtures/good_8of11.md` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/tests/fixtures/bad_hedges.md` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/skills/md-lint/tests/test_audit.py` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/hooks/md-lint-pre-write.sh` — Create
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/settings.json` — Modify (register hook)
- `D:/Users/Administrator/Documents/GitHub/orchestrator/SOUL/public/prompts/rationalization-immunity.md` — Modify (add Reframe Detection section)
- `D:/Users/Administrator/Documents/GitHub/orchestrator/CLAUDE.md` — Modify (add Tier Label Vocabulary section)
- `D:/Users/Administrator/Documents/GitHub/orchestrator/.claude/boot.md` — Modify (3-pass redundancy for rollback rule)

## Steps

### Phase A — Linter core (P0 #1 + #7 + #8 + #9)

1. Create `.claude/skills/md-lint/scripts/audit.py` with regex tables copied from opus-mind `audit.py` lines 50-150: `NARRATION`, `HEDGES`, `LADDER_SIGNALS`, `REFRAME_SIGNALS`, `CONSEQUENCE_SIGNALS`, `EXAMPLE_SIGNALS`, `RATIONALE_SIGNALS`, `NUMBER_CONSTRAINT`, `DIRECTIVE_VERBS`, `XML_OPEN`, `XML_CLOSE`, `TIER_LABEL_RE`, `PLACEHOLDER_RE`, `NEGATOR_PATTERNS`, `_QUOTE_SPAN_RE`. Add `_iter_violations()` with negator + quote guard and `_iter_findings()` for presence-only scans.
   → verify: `python3 .claude/skills/md-lint/scripts/audit.py --help | grep "deterministic"`

2. Add 11 invariant scoring functions (I1-I11) to `audit.py` using thresholds from opus-mind: I1 hedge_density ≤ 0.25 + number_density ≥ 0.10, I2 ladder required when directives ≥ 6, I3 reframe required when refusal_topic ≥ 2, I4 narration == 0, I5 rationale ≥ 1 if examples, I6 consequences ≥ directives/10, I7 xml unmatched == 0, I8 default+exception pair ≥ 1, I9 self-check required if directives ≥ 12, I10 tier label ≥ 1, I11 hierarchy chain or Tier N tokens.
   - depends on: step 1
   → verify: `python3 .claude/skills/md-lint/scripts/audit.py CLAUDE.md --json | python3 -c "import sys,json;d=json.load(sys.stdin);assert 'pass' in d and len(d['pass'])==11"`

3. Add verdict logic (THIN/POOR/BORDERLINE/GOOD) to `audit.py`: THIN if line_count < 10 OR directives < 3; POOR if score < 6/11; BORDERLINE if 6-9/11; GOOD if ≥ 10/11. Add `placeholder_count` field via `PLACEHOLDER_RE`.
   - depends on: step 2
   → verify: `python3 .claude/skills/md-lint/scripts/audit.py CLAUDE.md --json | python3 -c "import sys,json;d=json.load(sys.stdin);assert d['verdict'] in ['THIN','POOR','BORDERLINE','GOOD']"`

4. Create `.claude/skills/md-lint/tests/fixtures/good_8of11.md` by hand: 40 lines, 1 xml block, 2 decision ladders (Step 0/Step 1), 3 hard numbers, 1 self-check block, 1 rationale, 3 consequence words, 1 `<critical>` tier label, 1 "Tier 1/Tier 2" hierarchy, zero narration, zero hedges.
   - depends on: step 3
   → verify: `python3 .claude/skills/md-lint/scripts/audit.py .claude/skills/md-lint/tests/fixtures/good_8of11.md --json | python3 -c "import sys,json;d=json.load(sys.stdin);assert sum(d['pass'].values())>=8, d['pass']"`

5. Create `.claude/skills/md-lint/tests/fixtures/bad_hedges.md` with 10 directives and 5 hedge words ("probably", "might", "when appropriate") to fail I1.
   - depends on: step 3
   → verify: `python3 .claude/skills/md-lint/scripts/audit.py .claude/skills/md-lint/tests/fixtures/bad_hedges.md --json | python3 -c "import sys,json;d=json.load(sys.stdin);assert not d['pass']['I1_reduce_interpretation']"`

6. Create `tests/test_audit.py` with pytest cases: good fixture passes ≥ 8/11, bad fixture fails I1, `\"Claude does not say 'let me'\"` does NOT flag I4 (negator guard), placeholder `<FIXME>` increments `placeholder_count`.
   - depends on: step 4, step 5
   → verify: `cd .claude/skills/md-lint && python3 -m pytest tests/test_audit.py -v`

### Phase B — Self-audit gate (P0 #2)

7. Create `.claude/hooks/md-lint-pre-write.sh`: reads hook stdin JSON, if target path matches `.claude/skills/*/SKILL.md` OR `CLAUDE.md`, runs `audit.py --json <path>` on the NEW content (from hook payload), blocks with exit 2 + stderr message if `sum(pass.values()) < 8`.
   - depends on: step 3
   → verify: `echo '{"tool_input":{"file_path":".claude/skills/md-lint/tests/fixtures/bad_hedges.md"}}' | bash .claude/hooks/md-lint-pre-write.sh; test $? -eq 2`

8. Modify `.claude/settings.json`: add `hooks.PreToolUse` entry matching `Write|Edit` with `.claude/skills/*/SKILL.md` or `CLAUDE.md` pattern, command `bash .claude/hooks/md-lint-pre-write.sh`.
   - depends on: step 7
   → verify: `python3 -c "import json; s=json.load(open('.claude/settings.json')); assert any('md-lint' in str(h) for h in s.get('hooks',{}).get('PreToolUse',[]))"`

### Phase C — Rule-text patches (P0 #3 + #4 + #5)

9. Append a "Reframe Detection" section to `SOUL/public/prompts/rationalization-immunity.md` with: cue layer ("只是一个小" / "这种情况不算" / "从另一个角度看" / "就这一次"), 3 concrete examples (one safety-related, one git-rollback-related, one deletion-related), rationale ("软化 = 触发，不是路径"), action ("停止当前操作，复述 owner 明确指令，若指令缺失则 ask"). Length 30-50 lines.
   → verify: `grep -c "Reframe Detection\|软化 = 触发\|cue:" SOUL/public/prompts/rationalization-immunity.md | awk '$1>=3'`

10. Add a "Tier Label Vocabulary" section to `CLAUDE.md` immediately before `<critical>` block with 4-level vocabulary: `NEVER`（破坏性系统操作: rm -rf /, drop database, kill orchestrator container）, `SEVERE VIOLATION`（越权: 未授权 rollback / 跳过 Gate / 跨 steal 分支）, `HARD LIMIT`（数字阈值：文件 > 300 LOC 必先 delete-before-rebuild; skill 评分 < 8/11 block commit）, `DEFAULT`（一般偏好: 中文沟通, Chinese response）. Add instruction: each existing Gate Function step must be re-classified under one label.
    → verify: `grep -c "^## Tier Label Vocabulary\|NEVER\|SEVERE VIOLATION\|HARD LIMIT\|DEFAULT" CLAUDE.md | awk '$1>=5'`

11. Apply 3-pass redundancy for the rollback rule: (a) `boot.md` append 1-line identity-level declaration "rollback 是禁区（唯一例外: owner 明确 says 'roll back'）"; (b) `CLAUDE.md` already has the formal rule in `<critical>`, leave it; (c) create Gate Function step self-check question "`Did the owner explicitly say 'roll back', 'reset', or 'revert'? → NO: STOP`" (already present, confirm existence by grep).
    → verify: `grep -c "rollback\|roll back" .claude/boot.md CLAUDE.md | awk -F: '{s+=$2} END {exit (s>=4)?0:1}'`

### Phase D — Dogfood + bootstrap (P0 #2 验证 self-audit)

12. Run `python3 .claude/skills/md-lint/scripts/audit.py .claude/skills/md-lint/SKILL.md --json` on the newly-created skill's own SKILL.md. If score < 8/11, rewrite SKILL.md until it passes. This is the self-audit gate test — the linter must pass its own review.
    - depends on: step 6
    → verify: `python3 .claude/skills/md-lint/scripts/audit.py .claude/skills/md-lint/SKILL.md --json | python3 -c "import sys,json;d=json.load(sys.stdin);s=sum(d['pass'].values());assert s>=8, f'self-audit failed: {s}/11'"`

13. Run linter on all 7 existing skills + CLAUDE.md to establish baseline: `for f in .claude/skills/*/SKILL.md CLAUDE.md; do python3 .claude/skills/md-lint/scripts/audit.py "$f" --json; done > baseline.json`. Record each file's score in a new `docs/steal/R79-opus-mind-baseline.md`.
    - depends on: step 3
    → verify: `test -f docs/steal/R79-opus-mind-baseline.md && grep -c "I1_\|I2_\|verdict" docs/steal/R79-opus-mind-baseline.md | awk '$1>=8'`

## Phase Gates

--- PHASE GATE: A → B ---
[ ] Deliverable exists: `audit.py` returns valid JSON on CLAUDE.md
[ ] Acceptance criteria met: all 11 invariant checks implemented, fixtures pass/fail as designed, negator guard prevents false positive
[ ] No open questions: threshold values copied from opus-mind verbatim (tune later after baseline)
[ ] Owner review: not required — reversible, no external effects

--- PHASE GATE: B → C ---
[ ] Deliverable exists: hook blocks bad_hedges.md fixture on simulated write
[ ] Acceptance criteria met: settings.json registers hook, exit code 2 observed
[ ] No open questions: hook scope limited to SKILL.md + CLAUDE.md, no repo-wide lint
[ ] Owner review: **REQUIRED** — hooks affect every Write/Edit; show owner the hook code before enabling

--- PHASE GATE: C → D ---
[ ] Deliverable exists: 3 files edited (rationalization-immunity.md, CLAUDE.md, boot.md)
[ ] Acceptance criteria met: grep counts match, no banned placeholder phrases
[ ] No open questions: tier vocabulary is 4-level exactly (don't inflate)
[ ] Owner review: not required — content changes are revertible

--- PHASE GATE: D → DONE ---
[ ] Deliverable exists: baseline report + self-audit pass
[ ] Acceptance criteria met: linter passes its own audit ≥ 8/11, baseline records all 8 existing files
[ ] No open questions: scores for failing files are ranked for follow-up work
[ ] Owner review: **REQUIRED** — baseline shows which of our existing skills score worst; owner decides fix priority

## Effort Estimate

- Phase A: ~6h (linter core)
- Phase B: ~2h (hook + settings)
- Phase C: ~3h (text patches, careful wording)
- Phase D: ~1.5h (dogfood + baseline report)
- **Total: ~12.5h**

Justification for exceeding P0 "< 2h per pattern" soft cap: the 5 P0 patterns share 70% infrastructure (audit.py core). Implementing them as a bundle is strictly cheaper than 5 separate efforts.

## Deferred (out of scope for this plan)

- P1 #6 BOOST user-prompt coach (10-slot) — separate skill, future plan
- P1 #10 cue+example+rationale backfill for existing 12 rationalizations — handled by Phase C step 9 partially; remaining 11 entries deferred
- P2 items (anti-slop rewriter, XML hierarchy migration) — not worth current effort
