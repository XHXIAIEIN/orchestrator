# Core (required)
anthropic>=0.40.0
claude-agent-sdk>=0.1.0
anyio>=4.0.0
python-dotenv>=1.0.0
apscheduler
websockets>=14.0
PyYAML>=6.0
cryptography>=42.0.0

# Audio transcription (voice messages)
faster-whisper>=1.0.0

# Dev
pytest>=8.0.0

# Optional: system monitoring backpressure (src/core/system_monitor.py)
psutil>=5.9.0

# Vector memory layer (Qdrant + Ollama embedding)
qdrant-client>=1.9.0
httpx>=0.27.0

# GUI Engine (src/desktop_use/) — Windows only, not installed in Docker
# Install separately: pip install -r requirements-gui.txt
# mss>=9.0.0
# pyautogui>=0.9.54
# pytesseract>=0.3.10
# Pillow>=10.0.0
# rapidfuzz>=3.0.0
# pynput>=1.7.6
# pywinauto>=0.6.8

# Channels adapter package (extracted from src/channels/ — see docs/superpowers/plans/2026-04-17-channels-extraction.md)
-e ../orchestrator-channels
