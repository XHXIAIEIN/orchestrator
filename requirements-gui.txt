# GUI Engine (src/gui/) — Windows only
# These packages require native dependencies (evdev, X11) that don't build in Docker Linux containers
mss>=9.0.0
pyautogui>=0.9.54
pytesseract>=0.3.10
Pillow>=10.0.0
rapidfuzz>=3.0.0
pynput>=1.7.6
pywinauto>=0.6.8
