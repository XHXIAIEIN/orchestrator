# Desktop Use Engine (src/desktop_use/) — Windows only
# These packages require native dependencies that don't build in Docker Linux containers
cvui @ git+https://github.com/XHXIAIEIN/cvui.git  # UI element detection pipeline
mss>=9.0.0
pyautogui>=0.9.54
winocr>=0.2.0               # system-native OCR (WinRT)
Pillow>=10.0.0
rapidfuzz>=3.0.0
pynput>=1.7.6
pywinauto>=0.6.8
opencv-python-headless>=4.8.0  # CV edge detection for blueprint analysis
