# guideline: git-safety
## Trigger Conditions
Keywords: git, commit, branch, merge, rebase, stash, reset
## Rules
- Run git status before committing to confirm the scope of changes
- Never force push
- Do not commit directly to the main branch (if the project has a branching strategy)
- Commit message format: type(scope): description
