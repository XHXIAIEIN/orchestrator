# guideline: git-safety
## 触发条件
关键词: git, commit, branch, merge, rebase, stash, reset
## 规则
- commit 前先 git status 确认改动范围
- 不要 force push
- 不要在 main 分支直接提交（如果项目有分支策略）
- commit message 格式: type(scope): description
