# guideline: dependency
## Trigger Conditions
Keywords: dependency, requirements, import, new package, install, pip
## Rules
- Do not introduce new dependencies unless the task explicitly requires it
- If a new dependency is necessary, check requirements.txt and add it there
- Prefer the standard library whenever possible
