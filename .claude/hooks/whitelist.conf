# Guard Hook — Trusted Domains Whitelist
# Domains listed here are exempt from exfiltration checks (curl POST, wget POST, etc.)
# One regex pattern per line. Lines starting with # are comments. Blank lines ignored.
# These are domains the agent legitimately needs to POST to.

# Local development
localhost
127\.0\.0\.1

# Clawvard exam API
clawvard\.school

# Telegram Bot API
api\.telegram\.org

# WeChat iLink Bot API
ilinkai\.weixin\.qq\.com
